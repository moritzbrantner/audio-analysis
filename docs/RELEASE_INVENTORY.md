# Release inventory

The reviewed extraction contains 53 Cargo packages and 26 Bun packages. Their
copied versions are preserved; release versions and ordering require a later
exact release issue and checked-in release manifest.

The destination-authored private `@moritzbrantner/audio-app-ui` package is not
automatically publishable. Issue #2 authorizes no Cargo or npm publication.
