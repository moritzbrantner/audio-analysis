# ADR 0010: Speaker-conditioned TTS family

`moenarch-audio-generation-tts` owns speaker-conditioned request/response contracts, Reference Voice Prompt planning, explicit model-preset metadata, and provider diagnostics. Default builds are model-free, network-free, and do not silently select a model. Consent and identity policy remain downstream-owned.
